<Window x:Class="ClubManagement.ClubDetailPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClubManagement"
        mc:Ignorable="d"
        Title="ClubDetailPage" Height="450" Width="800">
	<Grid>
		<Grid.ColumnDefinitions>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="60px"/>
			<RowDefinition Height="*"/>

		</Grid.RowDefinitions>

		<Grid>
			<Menu Grid.Row="0" VerticalAlignment="Top" FontSize="16" Height="60" Margin="70,0,61,0">
				<Menu.ItemsPanel>
					<ItemsPanelTemplate>
						<UniformGrid Columns="4"/>
					</ItemsPanelTemplate>
				</Menu.ItemsPanel>
				<MenuItem Header="게시글" FontSize="16" HorizontalAlignment="Center">
					<!-- 게시글 관련 기능 추가 -->
				</MenuItem>
				<MenuItem Header="부원 관리" FontSize="16" HorizontalAlignment="Center">
					<!-- 부원 관리 관련 기능 추가 -->
				</MenuItem>
				<MenuItem Header="수정" FontSize="16" HorizontalAlignment="Center">
					<!-- 수정 기능 추가 -->
				</MenuItem>
				<MenuItem Header="삭제" FontSize="16" HorizontalAlignment="Center">
					<!-- 삭제 기능 추가 -->
				</MenuItem>
			</Menu>
		</Grid>

	</Grid>
</Window>
