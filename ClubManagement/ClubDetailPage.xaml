<Window x:Class="ClubManagement.ClubDetailPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ClubManagement"
        
        mc:Ignorable="d"
        Title="ClubDetailPage" Height="450" Width="800">
    

    <Grid>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="Gray">
            <Grid>
                
                    <Image Source="/Resources/kumohImg.jpg" HorizontalAlignment="Left" Width="50"/>
                
                
                <Menu Grid.Row="0" VerticalAlignment="Top" FontSize="16" Height="60" Margin="70,0,61,0" Background="Transparent">
                    <Menu.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4"/>
                        </ItemsPanelTemplate>
                    </Menu.ItemsPanel>
                    <MenuItem Header="게시글" FontSize="16" HorizontalAlignment="Center">
                        <!-- 게시글 관련 기능 추가 -->
                    </MenuItem>
                    <MenuItem Header="부원 관리" FontSize="16" HorizontalAlignment="Center">
                        <!-- 부원 관리 관련 기능 추가 -->
                    </MenuItem>
                    <MenuItem Header="수정" FontSize="16" HorizontalAlignment="Center">
                        <!-- 수정 기능 추가 -->
                    </MenuItem>
                    <MenuItem Header="삭제" FontSize="16" HorizontalAlignment="Center">
                        <!-- 삭제 기능 추가 -->
                    </MenuItem>
                </Menu>
                <Button Width="80" Content="뒤로 가기" HorizontalAlignment="Right" Background="Transparent">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="30"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
            </Grid>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>
                
                
                <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1,0,1,1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Source="/Resources/soccerball.jpg" HorizontalAlignment="Center" Width="50"/>
                        <TextBlock  FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center">동아리명</TextBlock>
                    </StackPanel>
                    
                </Border>
               


               
                <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1,0,1,1">
                    <TextBlock FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center">한줄소개</TextBlock>
                </Border>

                
                <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1,0,1,1">
                    <TextBlock FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Top">설명글</TextBlock>
                </Border>
                
            </Grid>


            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Previous" Click="PreviousMonth_Click"/>
                    <TextBlock Text="{Binding DisplayedMonth}" Margin="10"/>
                    <Button Content="Next" Click="NextMonth_Click"/>
                </StackPanel>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Dates}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="6" Columns="7"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Black" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Day}"/>
                                    <!-- Add other data to display for the date -->
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

        </Grid>
    </Grid>
</Window>
