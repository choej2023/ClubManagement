<Window x:Class="ClubManagement.PostCreatePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClubManagement"
        mc:Ignorable="d"
        Title="PostCreatePage" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="제목" Margin="10"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="1" Margin="10"/>

        <TextBlock Text="내용" Grid.Row="2" Margin="10"/>
        <TextBox x:Name="ContentTextBox" Grid.Row="3" Margin="10" AcceptsReturn="True" Height="150"/>

        <TextBlock Text="파일" Grid.Row="4" Margin="10"/>
        <Button Content="파일 선택" Grid.Row="5" HorizontalAlignment="Left" Margin="10" Width="100" Click="SelectFile_Click"/>
        <TextBlock x:Name="FilePathTextBlock" Grid.Row="5" Margin="120,10,10,10" VerticalAlignment="Center"/>

        <Button Content="등록" Grid.Row="6" HorizontalAlignment="Right" Margin="10" Width="100" Click="Register_Click"/>
    </Grid>


</Window>
